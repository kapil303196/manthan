<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Show Finder</title>
</head>

<body>
  <main>
    <section>
      <h1>shows</h1>
      {{#if shows.length }}
      <ul>
        {{#each shows}}
        <li>
          <a href="/shows/{{show.id}}">{{show.name}}</a>
        </li>
        {{/each}}
      </ul>
      {{else}} {{#if query }}
      <p class="not-found">We're sorry, but no results were found for {{query.searchTerm}}.</p>
      {{/if}}
      {{/if}}
    </section>
  </main>
</body>

</html>